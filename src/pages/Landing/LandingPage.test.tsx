import { render, screen } from '@testing-library/react'
import LandingPage from './LandingPage'

test('LandingPage Loaded', () => {
  // render(<LandingPage />)
  // console.log('have landing')
  // const a = screen.getByText('Survey')
  // expect(a).toBe(true)
  // expect(img).toHaveAttribute('alt', 'img')
})
